# Simple React Project!

A simple React Native app built to learn the fundamentals of React Native and get hands-on with data persistence using AsyncStorage. Written without any generative AI assistance. Includes some seemingly arbitrary constraints! these are intentional, in prep for a larger upcoming project (see the comments for all the context).
